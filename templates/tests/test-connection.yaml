apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "minecraft-overviewer.fullname" . }}-test-connection"
  labels:
    {{- include "minecraft-overviewer.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "minecraft-overviewer.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
